// Template for poll/config.js (which is gitignored — no keys are committed).
//
// Either copy this file to config.js and fill in the values, or run ./gen-config.sh
// to generate config.js from ../.env (SUPABASE_URL, SUPABASE_KEY).
window.SODA_POLL_CONFIG = {
  supabaseUrl: "",   // e.g. https://<project-ref>.supabase.co
  supabaseKey: "",   // Supabase publishable/anon key
};
